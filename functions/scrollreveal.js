import ScrollReveal from "scrollreveal";

const scrollReveal = () => {
	ScrollReveal().reveal('.profile-main-container', { delay: 725, origin: 'top', interval: 100, distance: '60px', duration: 2500, reset: true });
};

export default scrollReveal;